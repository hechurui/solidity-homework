# MetaNode Stake 使用示例

本文档提供完整的代码示例，展示如何与 MetaNode Stake 系统交互。

## 📋 目录

1. [基础设置](#基础设置)
2. [查询操作](#查询操作)
3. [质押操作](#质押操作)
4. [解质押和提取](#解质押和提取)
5. [奖励领取](#奖励领取)
6. [Web3.js 示例](#web3js-示例)
7. [React 集成示例](#react-集成示例)

---

## 基础设置

### Hardhat 脚本设置

```javascript
// scripts/examples/basic-setup.js
const hre = require("hardhat");

async function main() {
  // 获取签名者
  const [deployer, user1, user2] = await ethers.getSigners();

  // 合约地址 (替换为你的部署地址)
  const STAKE_POOL_ADDRESS = "0x...";
  const META_TOKEN_ADDRESS = "0x...";
  const TEST_TOKEN_ADDRESS = "0x...";

  // 连接到已部署的合约
  const StakePool = await ethers.getContractFactory("StakePool");
  const stakePool = StakePool.attach(STAKE_POOL_ADDRESS);

  const MetaToken = await ethers.getContractFactory("MetaNodeToken");
  const metaToken = MetaToken.attach(META_TOKEN_ADDRESS);

  const TestToken = await ethers.getContractFactory("TestToken");
  const testToken = TestToken.attach(TEST_TOKEN_ADDRESS);

  console.log("合约连接成功!");
  console.log("StakePool:", stakePool.address);
  console.log("MetaToken:", metaToken.address);
  console.log("TestToken:", testToken.address);

  return { stakePool, metaToken, testToken, deployer, user1, user2 };
}

main()
  .then(() => process.exit(0))
  .catch((error) => {
    console.error(error);
    process.exit(1);
  });
```

---

## 查询操作

### 1. 查询池信息

```javascript
// scripts/examples/query-pools.js
async function queryPools(stakePool) {
  console.log("\n========== 查询池信息 ==========\n");

  // 获取池数量
  const poolLength = await stakePool.getPoolLength();
  console.log(`总池数: ${poolLength}`);

  // 遍历所有池
  for (let i = 0; i < poolLength; i++) {
    const pool = await stakePool.pools(i);

    console.log(`\n池 ${i}:`);
    console.log(`  质押代币: ${pool.stTokenAddress === ethers.ZeroAddress ? "ETH" : pool.stTokenAddress}`);
    console.log(`  池权重: ${pool.poolWeight}`);
    console.log(`  总质押量: ${ethers.formatEther(pool.stTokenAmount)} 代币`);
    console.log(`  最小质押: ${ethers.formatEther(pool.minDepositAmount)} 代币`);
    console.log(`  锁定区块数: ${pool.unstakeLockedBlocks}`);
    console.log(`  激活状态: ${pool.isActive ? "是" : "否"}`);
    console.log(`  上次奖励区块: ${pool.lastRewardBlock}`);
    console.log(`  累积奖励: ${pool.accMetaNodePerST}`);
  }
}

// 使用示例
const { stakePool } = await main();
await queryPools(stakePool);
```

### 2. 查询用户信息

```javascript
// scripts/examples/query-user.js
async function queryUserInfo(stakePool, poolId, userAddress) {
  console.log(`\n========== 用户信息 (池 ${poolId}) ==========\n`);

  // 获取用户信息
  const userInfo = await stakePool.getUserInfo(poolId, userAddress);

  console.log(`质押数量: ${ethers.formatEther(userInfo.stAmount)} 代币`);
  console.log(`待领取奖励: ${ethers.formatEther(userInfo.pendingReward)} META`);

  // 解质押请求
  console.log(`\n解质押请求 (${userInfo.requests.length} 个):`);
  if (userInfo.requests.length > 0) {
    const currentBlock = await ethers.provider.getBlockNumber();

    for (let i = 0; i < userInfo.requests.length; i++) {
      const request = userInfo.requests[i];
      const blocksLeft = request.unlockBlock > currentBlock
        ? request.unlockBlock - currentBlock
        : 0;

      console.log(`  请求 ${i + 1}:`);
      console.log(`    数量: ${ethers.formatEther(request.amount)} 代币`);
      console.log(`    解锁区块: ${request.unlockBlock}`);
      console.log(`    剩余区块: ${blocksLeft} ${blocksLeft === 0 ? "(可提取)" : ""}`);
    }
  }

  // 可提取数量
  const withdrawable = await stakePool.getWithdrawableAmount(poolId, userAddress);
  console.log(`\n可提取数量: ${ethers.formatEther(withdrawable)} 代币`);
}

// 使用示例
const { stakePool, user1 } = await main();
await queryUserInfo(stakePool, 0, user1.address);
```

### 3. 查询奖励信息

```javascript
// scripts/examples/query-rewards.js
async function queryRewards(stakePool, poolId, userAddress) {
  console.log(`\n========== 奖励信息 ==========\n`);

  // 查询待领取奖励
  const pending = await stakePool.pendingMetaNode(poolId, userAddress);
  console.log(`待领取奖励: ${ethers.formatEther(pending)} META`);

  // 查询全局奖励率
  const metaNodePerBlock = await stakePool.metaNodePerBlock();
  console.log(`每区块奖励: ${ethers.formatEther(metaNodePerBlock)} META`);

  // 计算用户每区块可得奖励
  const pool = await stakePool.pools(poolId);
  const userInfo = await stakePool.getUserInfo(poolId, userAddress);
  const totalPoolWeight = await stakePool.totalPoolWeight();

  if (pool.stTokenAmount > 0n) {
    const poolRewardPerBlock = (metaNodePerBlock * pool.poolWeight) / totalPoolWeight;
    const userRewardPerBlock = (poolRewardPerBlock * userInfo.stAmount) / pool.stTokenAmount;

    console.log(`池每区块奖励: ${ethers.formatEther(poolRewardPerBlock)} META`);
    console.log(`您每区块奖励: ${ethers.formatEther(userRewardPerBlock)} META`);
  }
}

// 使用示例
const { stakePool, user1 } = await main();
await queryRewards(stakePool, 0, user1.address);
```

---

## 质押操作

### 1. 质押 ETH

```javascript
// scripts/examples/stake-eth.js
async function stakeETH(stakePool, poolId, amount) {
  console.log(`\n========== 质押 ETH ==========\n`);

  const amountWei = ethers.parseEther(amount.toString());

  // 检查最小质押量
  const pool = await stakePool.pools(poolId);
  if (amountWei < pool.minDepositAmount) {
    throw new Error(`质押数量低于最小值: ${ethers.formatEther(pool.minDepositAmount)} ETH`);
  }

  console.log(`质押数量: ${amount} ETH`);
  console.log(`池 ID: ${poolId}`);

  // 执行质押
  const tx = await stakePool.stake(poolId, amountWei, { value: amountWei });
  console.log(`交易哈希: ${tx.hash}`);

  const receipt = await tx.wait();
  console.log(`✅ 质押成功! Gas 使用: ${receipt.gasUsed.toString()}`);

  // 查询更新后的信息
  const userInfo = await stakePool.getUserInfo(poolId, await stakePool.signer.getAddress());
  console.log(`当前质押: ${ethers.formatEther(userInfo.stAmount)} ETH`);
}

// 使用示例
const { stakePool } = await main();
await stakeETH(stakePool, 0, 0.1); // 质押 0.1 ETH 到池 0
```

### 2. 质押 ERC20

```javascript
// scripts/examples/stake-erc20.js
async function stakeERC20(stakePool, testToken, poolId, amount) {
  console.log(`\n========== 质押 ERC20 ==========\n`);

  const amountWei = ethers.parseEther(amount.toString());

  // 1. 检查余额
  const userAddress = await stakePool.signer.getAddress();
  const balance = await testToken.balanceOf(userAddress);
  console.log(`当前余额: ${ethers.formatEther(balance)} TST`);

  if (balance < amountWei) {
    throw new Error("余额不足");
  }

  // 2. 授权
  console.log(`\n步骤 1: 授权 ${amount} TST...`);
  const approveTx = await testToken.approve(stakePool.address, amountWei);
  await approveTx.wait();
  console.log(`✅ 授权成功`);

  // 3. 质押
  console.log(`\n步骤 2: 质押 ${amount} TST...`);
  const stakeTx = await stakePool.stake(poolId, amountWei);
  const receipt = await stakeTx.wait();
  console.log(`✅ 质押成功! Gas 使用: ${receipt.gasUsed.toString()}`);

  // 4. 查询更新后的信息
  const userInfo = await stakePool.getUserInfo(poolId, userAddress);
  console.log(`当前质押: ${ethers.formatEther(userInfo.stAmount)} TST`);
}

// 使用示例
const { stakePool, testToken } = await main();
await stakeERC20(stakePool, testToken, 1, 100); // 质押 100 TST 到池 1
```

---

## 解质押和提取

### 1. 发起解质押

```javascript
// scripts/examples/unstake.js
async function unstake(stakePool, poolId, amount) {
  console.log(`\n========== 发起解质押 ==========\n`);

  const amountWei = ethers.parseEther(amount.toString());
  const userAddress = await stakePool.signer.getAddress();

  // 检查质押余额
  const userInfo = await stakePool.getUserInfo(poolId, userAddress);
  console.log(`当前质押: ${ethers.formatEther(userInfo.stAmount)} 代币`);

  if (userInfo.stAmount < amountWei) {
    throw new Error("质押余额不足");
  }

  // 发起解质押
  console.log(`解质押数量: ${amount} 代币`);
  const tx = await stakePool.unstake(poolId, amountWei);
  const receipt = await tx.wait();
  console.log(`✅ 解质押请求已创建! Gas 使用: ${receipt.gasUsed.toString()}`);

  // 查询解锁区块
  const pool = await stakePool.pools(poolId);
  const currentBlock = await ethers.provider.getBlockNumber();
  const unlockBlock = currentBlock + Number(pool.unstakeLockedBlocks);

  console.log(`\n解锁信息:`);
  console.log(`  当前区块: ${currentBlock}`);
  console.log(`  解锁区块: ${unlockBlock}`);
  console.log(`  需要等待: ${pool.unstakeLockedBlocks} 个区块`);

  // 估算解锁时间 (假设 12 秒一个区块)
  const estimatedMinutes = (Number(pool.unstakeLockedBlocks) * 12) / 60;
  console.log(`  预计时间: ~${estimatedMinutes.toFixed(1)} 分钟`);
}

// 使用示例
const { stakePool } = await main();
await unstake(stakePool, 0, 0.05); // 解质押 0.05 ETH
```

### 2. 提取质押代币

```javascript
// scripts/examples/withdraw.js
async function withdraw(stakePool, poolId) {
  console.log(`\n========== 提取质押代币 ==========\n`);

  const userAddress = await stakePool.signer.getAddress();

  // 检查可提取数量
  const withdrawable = await stakePool.getWithdrawableAmount(poolId, userAddress);
  console.log(`可提取数量: ${ethers.formatEther(withdrawable)} 代币`);

  if (withdrawable === 0n) {
    console.log("⚠️  没有可提取的代币 (可能还在锁定期)");
    return;
  }

  // 执行提取
  const tx = await stakePool.withdraw(poolId);
  const receipt = await tx.wait();
  console.log(`✅ 提取成功! Gas 使用: ${receipt.gasUsed.toString()}`);

  // 查询更新后的请求列表
  const userInfo = await stakePool.getUserInfo(poolId, userAddress);
  console.log(`剩余解质押请求: ${userInfo.requests.length} 个`);
}

// 使用示例
const { stakePool } = await main();
await withdraw(stakePool, 0);
```

### 3. 紧急提取

```javascript
// scripts/examples/emergency-withdraw.js
async function emergencyWithdraw(stakePool, poolId) {
  console.log(`\n========== 紧急提取 ==========\n`);
  console.log(`⚠️  警告: 紧急提取将放弃所有奖励!`);

  const userAddress = await stakePool.signer.getAddress();
  const userInfo = await stakePool.getUserInfo(poolId, userAddress);

  console.log(`将提取: ${ethers.formatEther(userInfo.stAmount)} 代币`);
  console.log(`将放弃: ${ethers.formatEther(userInfo.pendingReward)} META 奖励`);

  // 执行紧急提取
  const tx = await stakePool.emergencyWithdraw(poolId);
  const receipt = await tx.wait();
  console.log(`✅ 紧急提取成功! Gas 使用: ${receipt.gasUsed.toString()}`);
}

// 使用示例 (谨慎使用!)
const { stakePool } = await main();
// await emergencyWithdraw(stakePool, 0);
```

---

## 奖励领取

### 1. 查询并领取奖励

```javascript
// scripts/examples/claim-rewards.js
async function claimRewards(stakePool, poolId) {
  console.log(`\n========== 领取奖励 ==========\n`);

  const userAddress = await stakePool.signer.getAddress();

  // 查询待领取奖励
  const pending = await stakePool.pendingMetaNode(poolId, userAddress);
  console.log(`待领取奖励: ${ethers.formatEther(pending)} META`);

  if (pending === 0n) {
    console.log("⚠️  暂无待领取奖励");
    return;
  }

  // 执行领取
  const tx = await stakePool.claim(poolId);
  const receipt = await tx.wait();
  console.log(`✅ 奖励领取成功! Gas 使用: ${receipt.gasUsed.toString()}`);

  // 验证领取
  const newPending = await stakePool.pendingMetaNode(poolId, userAddress);
  console.log(`剩余待领取: ${ethers.formatEther(newPending)} META`);
}

// 使用示例
const { stakePool } = await main();
await claimRewards(stakePool, 0);
```

### 2. 批量领取奖励

```javascript
// scripts/examples/claim-all.js
async function claimAllRewards(stakePool) {
  console.log(`\n========== 批量领取所有池的奖励 ==========\n`);

  const userAddress = await stakePool.signer.getAddress();
  const poolLength = await stakePool.getPoolLength();

  let totalRewards = 0n;

  // 遍历所有池
  for (let i = 0; i < poolLength; i++) {
    const pending = await stakePool.pendingMetaNode(i, userAddress);

    if (pending > 0n) {
      console.log(`\n池 ${i}:`);
      console.log(`  待领取: ${ethers.formatEther(pending)} META`);

      try {
        const tx = await stakePool.claim(i);
        await tx.wait();
        console.log(`  ✅ 领取成功`);
        totalRewards += pending;
      } catch (error) {
        console.log(`  ❌ 领取失败:`, error.message);
      }
    }
  }

  console.log(`\n========== 总计 ==========`);
  console.log(`总领取: ${ethers.formatEther(totalRewards)} META`);
}

// 使用示例
const { stakePool } = await main();
await claimAllRewards(stakePool);
```

---

## Web3.js 示例

### 前端页面集成

```javascript
// frontend/stake.js
const Web3 = require('web3');

// ABI (简化版，完整版从 artifacts 获取)
const STAKE_POOL_ABI = [
  "function stake(uint256 _pid, uint256 _amount) external payable",
  "function unstake(uint256 _pid, uint256 _amount) external",
  "function withdraw(uint256 _pid) external",
  "function claim(uint256 _pid) external",
  "function pendingMetaNode(uint256 _pid, address _user) external view returns (uint256)",
  "function getUserInfo(uint256 _pid, address _user) external view returns (uint256, uint256, tuple(uint256, uint256)[])"
];

class StakeInterface {
  constructor(provider, contractAddress) {
    this.web3 = new Web3(provider);
    this.contract = new this.web3.eth.Contract(STAKE_POOL_ABI, contractAddress);
  }

  // 连接钱包
  async connectWallet() {
    if (window.ethereum) {
      const accounts = await window.ethereum.request({
        method: 'eth_requestAccounts'
      });
      this.account = accounts[0];
      return this.account;
    }
    throw new Error("请安装 MetaMask");
  }

  // 质押 ETH
  async stakeETH(poolId, amount) {
    const amountWei = this.web3.utils.toWei(amount.toString(), 'ether');

    return await this.contract.methods
      .stake(poolId, amountWei)
      .send({
        from: this.account,
        value: amountWei
      });
  }

  // 查询奖励
  async getPendingRewards(poolId) {
    const pending = await this.contract.methods
      .pendingMetaNode(poolId, this.account)
      .call();

    return this.web3.utils.fromWei(pending, 'ether');
  }

  // 领取奖励
  async claimRewards(poolId) {
    return await this.contract.methods
      .claim(poolId)
      .send({ from: this.account });
  }
}

// 使用示例
const stakeInterface = new StakeInterface(
  window.ethereum,
  "0x..." // 你的合约地址
);

await stakeInterface.connectWallet();
await stakeInterface.stakeETH(0, 0.1);
const rewards = await stakeInterface.getPendingRewards(0);
console.log(`待领取奖励: ${rewards} META`);
```

---

## React 集成示例

### React Hook

```jsx
// hooks/useStake.js
import { useState, useEffect } from 'react';
import { ethers } from 'ethers';

export function useStake(contractAddress, abi) {
  const [account, setAccount] = useState(null);
  const [contract, setContract] = useState(null);
  const [userInfo, setUserInfo] = useState(null);

  // 连接钱包
  const connectWallet = async () => {
    if (window.ethereum) {
      const provider = new ethers.BrowserProvider(window.ethereum);
      const signer = await provider.getSigner();
      const address = await signer.getAddress();

      setAccount(address);

      const stakeContract = new ethers.Contract(
        contractAddress,
        abi,
        signer
      );
      setContract(stakeContract);

      return address;
    }
  };

  // 查询用户信息
  const fetchUserInfo = async (poolId) => {
    if (!contract || !account) return;

    const info = await contract.getUserInfo(poolId, account);
    const pending = await contract.pendingMetaNode(poolId, account);

    setUserInfo({
      stAmount: ethers.formatEther(info.stAmount),
      pendingReward: ethers.formatEther(pending),
      requests: info.requests
    });
  };

  // 质押
  const stake = async (poolId, amount) => {
    if (!contract) throw new Error("请先连接钱包");

    const amountWei = ethers.parseEther(amount.toString());
    const tx = await contract.stake(poolId, amountWei, { value: amountWei });
    await tx.wait();

    await fetchUserInfo(poolId);
  };

  // 领取奖励
  const claim = async (poolId) => {
    if (!contract) throw new Error("请先连接钱包");

    const tx = await contract.claim(poolId);
    await tx.wait();

    await fetchUserInfo(poolId);
  };

  return {
    account,
    userInfo,
    connectWallet,
    fetchUserInfo,
    stake,
    claim
  };
}
```

### React 组件

```jsx
// components/StakePanel.jsx
import React, { useState } from 'react';
import { useStake } from '../hooks/useStake';

export function StakePanel({ contractAddress, abi }) {
  const [poolId] = useState(0);
  const [stakeAmount, setStakeAmount] = useState('');

  const {
    account,
    userInfo,
    connectWallet,
    fetchUserInfo,
    stake,
    claim
  } = useStake(contractAddress, abi);

  const handleConnect = async () => {
    await connectWallet();
    await fetchUserInfo(poolId);
  };

  const handleStake = async () => {
    try {
      await stake(poolId, stakeAmount);
      alert('质押成功!');
      setStakeAmount('');
    } catch (error) {
      alert('质押失败: ' + error.message);
    }
  };

  const handleClaim = async () => {
    try {
      await claim(poolId);
      alert('领取成功!');
    } catch (error) {
      alert('领取失败: ' + error.message);
    }
  };

  return (
    <div className="stake-panel">
      {!account ? (
        <button onClick={handleConnect}>连接钱包</button>
      ) : (
        <div>
          <p>钱包地址: {account.slice(0, 6)}...{account.slice(-4)}</p>

          {userInfo && (
            <div className="user-info">
              <p>已质押: {userInfo.stAmount} ETH</p>
              <p>待领取: {userInfo.pendingReward} META</p>
            </div>
          )}

          <div className="stake-form">
            <input
              type="number"
              placeholder="质押数量 (ETH)"
              value={stakeAmount}
              onChange={(e) => setStakeAmount(e.target.value)}
            />
            <button onClick={handleStake}>质押</button>
          </div>

          <button onClick={handleClaim}>领取奖励</button>
        </div>
      )}
    </div>
  );
}
```

---

## 🎯 完整流程示例

### 从质押到领取的完整流程

```javascript
// scripts/examples/complete-flow.js
async function completeStakingFlow() {
  console.log("\n========================================");
  console.log("完整质押流程演示");
  console.log("========================================\n");

  const { stakePool, metaToken, testToken, user1 } = await main();

  // 1. 质押 ETH
  console.log("步骤 1: 质押 0.1 ETH");
  const stakeAmount = ethers.parseEther("0.1");
  await stakePool.connect(user1).stake(0, stakeAmount, { value: stakeAmount });
  console.log("✅ 质押成功\n");

  // 2. 等待几个区块累积奖励
  console.log("步骤 2: 等待奖励累积...");
  for (let i = 0; i < 10; i++) {
    await ethers.provider.send("evm_mine");
  }
  console.log("✅ 已挖掘 10 个区块\n");

  // 3. 查询奖励
  console.log("步骤 3: 查询奖励");
  const pending = await stakePool.pendingMetaNode(0, user1.address);
  console.log(`待领取奖励: ${ethers.formatEther(pending)} META\n`);

  // 4. 领取奖励
  console.log("步骤 4: 领取奖励");
  await stakePool.connect(user1).claim(0);
  const balance = await metaToken.balanceOf(user1.address);
  console.log(`✅ META 余额: ${ethers.formatEther(balance)}\n`);

  // 5. 发起解质押
  console.log("步骤 5: 发起解质押");
  await stakePool.connect(user1).unstake(0, stakeAmount);
  console.log("✅ 解质押请求已创建\n");

  // 6. 等待锁定期
  console.log("步骤 6: 等待锁定期...");
  for (let i = 0; i < 15; i++) {
    await ethers.provider.send("evm_mine");
  }
  console.log("✅ 锁定期已过\n");

  // 7. 提取质押代币
  console.log("步骤 7: 提取质押代币");
  const withdrawable = await stakePool.getWithdrawableAmount(0, user1.address);
  console.log(`可提取: ${ethers.formatEther(withdrawable)} ETH`);
  await stakePool.connect(user1).withdraw(0);
  console.log("✅ 提取成功\n");

  console.log("========================================");
  console.log("✅ 完整流程演示完成!");
  console.log("========================================");
}

// 执行
completeStakingFlow()
  .then(() => process.exit(0))
  .catch(error => {
    console.error(error);
    process.exit(1);
  });
```

---

## 📚 相关文档

- [部署教程](./deploy.md) - 如何部署合约
- [README.md](./README.md) - 项目概述
- [TEST_GUIDE.md](./TEST_GUIDE.md) - 测试指南

---

## 💡 最佳实践

1. **错误处理**: 始终使用 try-catch 包裹交易
2. **Gas 估算**: 在发送交易前估算 Gas
3. **事件监听**: 监听合约事件获取实时更新
4. **余额检查**: 交易前检查余额和授权
5. **用户体验**: 提供清晰的加载状态和错误信息

---
